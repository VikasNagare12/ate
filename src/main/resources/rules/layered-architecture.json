{
"id": "LAYERED-ARCHITECTURE-001",
"name": "Layered Architecture Violation",
"enabled": true,
"severity": "ERROR",
"confidence": "HIGH",
"description": "Service layer must not depend on Web/Controller layer",
"target": {
    "artifactType": "Method",
    "namePattern": ".*service\\..*"
},
"rationale": {
    "why": "Controllers should call Services, not the other way around.",
    "impact": "Tight coupling, cyclic dependencies, hard to test services in isolation."
},
"remediation": {
    "summary": "Remove dependency from Service to Controller.",
    "options": [
        "Move shared logic to a lower layer (e.g., Domain or Utility)",
        "Use events to communicate upwards"
    ]
}
}