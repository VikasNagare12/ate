{
  "id": "TX-BOUNDARY-001",
  "name": "No Remote Calls in Transactions",
  "description": "Methods annotated with @Transactional should not make remote HTTP calls (RestTemplate, WebClient) as this can cause long-running transactions and database lock issues.",
  "severity": "ERROR",
  "config": {
    "annotationRequired": "Transactional",
    "sinkPatterns": [
      "RestTemplate.getForObject",
      "RestTemplate.postForObject",
      "RestTemplate.exchange",
      "WebClient.get",
      "WebClient.post"
    ]
  }
}
